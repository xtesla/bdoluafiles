function FromClient_responseYachtDiceGameStop(hostPoint, acceptorPoint, isGameOver)
  if false == isGameOver then
  else
    PaGlobal_MiniGame_YachtDice_Result:prepareOpen(hostPoint, acceptorPoint)
  end
end
